<h5>Order a Burger</h5>
  <form action="/create" method="POST" class="button-size">
    <select class="browser-default" name="burgerInput">
      <option value="" disabled selected>Choose a burger</option>

      {{#each itm}}
        {{#if entree}}
          <option value="{{this.item}}">{{this.item}}</option>
        {{/if}}
      {{/each}}

    </select>
    <br>
    <button type="submit" class="waves-effect waves-light btn deep-purple lighten-1 z-depth-4">Add to Order</button>
  </form>

<h5>Add a Side</h5>
  <form action="/create" method="POST" class="button-size">
    <select class="browser-default" name="burgerInput">
      <option value="" disabled selected>Choose a side</option>

      {{#each itm}}
        {{#unless entree}}
          <option value="{{this.item}}">{{this.item}}</option>
        {{/unless}}
      {{/each}}

    </select>
    <br>
    <button type="submit" class="waves-effect waves-light btn deep-purple lighten-1 z-depth-4">Add to Order</button>
  </form>
